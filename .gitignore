# Ignore build outputs from performing a nix-build or `nix build` command
result
result-*

# Ignore automatically generated direnv output
.direnv
.DS_Store

# Encrypted keyfile for installer ISO (generated at build time)
hosts/installer/desktop.key.enc
