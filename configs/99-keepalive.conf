# Disable suspend on *all* audio sinks/sources (covers ALSA, Bluetooth, HDMI, etc.)
monitor.alsa.rules = [
  {
    matches = [
      { media.class = "Audio/Sink"  },
      { media.class = "Audio/Source" }
    ]
    actions = { update-props = { session.suspend-timeout-seconds = 0 } }
  }
]
